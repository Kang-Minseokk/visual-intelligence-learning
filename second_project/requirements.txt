# DINO ViT-S/8 SSL pretraining on STL-10 — reproduction requirements.
# Tested with Python 3.9.25, CUDA 12.4, RTX 4090 (24 GB).
# bf16 autocast requires Ampere (RTX 30xx) or newer GPU.

torch==2.5.1
torchvision==0.20.1
timm==1.0.11
numpy==2.0.1

# Optional (only needed for the TensorBoard visualization helper):
tensorboard==2.20.0
